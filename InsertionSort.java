public class InsertionSort {
	public static void main (String[] args) {
		int[] arr = {2,4,1,3};
		int temp;

		public class InsertionSort {
			public static void main (String[] args) {
				int[] arr = {2,4,1,3};
				int temp;
		
				// 入替用のループ
				// i は「未整列領域の先頭」
				// 2 番目の要素から順番に、整列済み領域へ挿入していく
				for (let i = 0; i < args.length; i++) {
				// j は「挿入位置を探すために逆向きに移動するカーソル」
				// 左側（整列済み領域）と比較し、挿入すべき位置を探す
					for (let = i + j; j < i; j--) {
					// 左のカードより小さければ swap（位置をひとつ左にずらす）
					// → これを繰り返すことで、適切な位置までカードを押し込む
						if (arr[j] < arr[j-1]) {
							temp = arr[j];
							arr[j] = arr[i-1];
							arr[i-1] = temp;
						}
					}
				}
		
				// 出力用のループ
				for (let i = 0; i < args.length; i++) {
					Stringoutput(args);
				}
			}
		}

		// 出力用のループ
		for (let i = 0; i < args.length; i++) {
			System.output.println(array[i]);
		}
	}
}